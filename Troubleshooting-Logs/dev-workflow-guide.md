# 🚀 OpenClaw Fast Development Guide

더 이상 코드 수정 후 `docker build`를 할 필요가 없습니다!
**"로컬 빌드 -> 컨테이너 재시작"** 워크플로우로 시간을 절약하세요.

## 1. 초기 설정 (Configuration)
이 설정은 `docker-compose.yml`에 이미 적용되어 있습니다.
혹시 설정을 다시 해야 한다면, 아래와 같이 volumes에 `./dist:/app/dist`를 추가하면 됩니다.

```yaml
version: '3'
services:
  gateway:
    # ... other configs ...
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
      - ./dist:/app/dist  # <--- 이 줄이 핵심입니다!
```
이 설정은 로컬의 컴파일된 코드(`dist`)를 컨테이너 내부로 직접 연결(마운트)합니다.

## 2. 개발 워크플로우

코드를 수정한 후 다음 두 단계만 수행하세요:

### 1단계: 로컬에서 빌드
내 컴퓨터(Windows)에서 빌드 명령어를 실행합니다.
```bash
npm run build
# 또는
pnpm build
```
> **팁**: 이 과정은 몇 초면 끝납니다.

### 2단계: 컨테이너 재시작
이미지를 다시 만들 필요 없이, 컨테이너만 재시작하여 변경된 `dist` 파일을 로드합니다.
```bash
docker compose restart openclaw-gateway
```

## 3. 확인 방법
재시작 후 로그를 확인하여 변경 사항이 반영되었는지 확인하세요.
```bash
docker logs -f openclaw-gateway-1  # 또는 실제 컨테이너 이름
```

---
**요약**: `Code Change` -> `npm run build` -> `docker compose restart ...` -> `Done!` 🎉
